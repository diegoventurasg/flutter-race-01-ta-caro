name: flutter_race_01_ta_caro
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_multi_formatter: ^2.4.4
  font_awesome_flutter: ^9.2.0
  google_fonts: ^2.1.0
  supabase: ^0.2.10
  validators: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
  assets:
    - assets/images/
